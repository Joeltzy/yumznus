
Router.route('/home', {
  template: 'home'
});

Router.route('/register', {
  template: 'register'
});

Router.route('/vote_list', {
  template: 'vote_list'
});

Router.route('/recommended', {
  template: 'recommended'
});

Router.route('/', {
  template: 'home'
});

Router.route('/signout', {
  template: 'signout'
});

Router.route('/login', {
  template: 'login'
});

